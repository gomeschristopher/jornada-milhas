export interface Promotion {
    id: number;
    destino: string;
    preco: number;
    imagem: string;
}

export interface State {
    nome: string;
}

export interface Testimonial {
    texto: string;
    autor: string;
}